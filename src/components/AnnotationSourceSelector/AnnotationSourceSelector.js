import React from 'react'; 
import Selector from './Selector/Selector';

const AnnotationSourceSelector = ({options})=>{
    return <Selector options = {options}/>
}

export default AnnotationSourceSelector;